# 如何像开发软件一样迭代

先读[产品架构](docs/product/architecture.md)、[模块数据契约](docs/product/contracts.md)和[协作契约](docs/product/collaboration.md)。这些是待实现产品的设计基线；现有命令行状态机仍以代码和测试为准。可使用仓库级 `career-gap-coach` skill 做人工试验，但 skill 不是网站运行时，也不能代替契约测试。

1. 提需求：说明用户、场景、当前问题、希望获得的结果。不放真人案例。
2. 建任务卡：写输入、产物、验收、负责人、依赖；有多人协作时先读相关 Issue 和评论，避免重复实现。
3. 拆成小改动：一条 PR 解决一个可验证问题；不同时重写流程、数据与界面。
4. 先添加能复现问题的测试，再修改实现。
5. 验收真实行为：状态不能越级，缺信息不能放行，重复运行不丢记录。
6. 运行 README 的测试、演示、隐私检查；人工检查将上传的文件与提交身份。
7. Review 后合并，更新版本、文档和未完成项。个人资料永远不进入协作内容。

任务从对应 GitHub Issue 认领；每条 PR 关联一个主 Issue，跨模块改字段先让受影响者 review。仓库的 Issue/PR 模板提醒来源、未知项和隐私风险，不应只为填模板而编造证据。

## 任务卡模板

- 目标：
- 输入与来源：
- 本次不做：
- 负责人：
- 产物路径：
- 验收命令与预期行为：
- 风险 / 待确认：
- 结果与证据：

无人认领不是完成；程序没崩不是输出正确；提交了 PR 不是已验收。

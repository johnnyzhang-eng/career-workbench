# 秋招加技能练习：首周虚构验收

关联 [#63](https://github.com/johnnyzhang-eng/career-workbench/issues/63)。2026-09-25，本机独立 `private/qa-study-20260925` 工作区；所有目标与结果均为虚构。未连接真实岗位，未上传简历或提交外部申请。

## 浏览器实际操作

1. 在本地目标工作台创建 300 分钟／周的虚构秋招目标，生成提案时勾选 SQL、Python、算法。
2. 首次浏览器操作确认生成 **14 项待决任务**；接受前目标仍显示“尚无生效计划”，没有每日任务。
3. 调整排序后重新用第二个虚构目标操作，提案首六项按“D1 求职→D1 SQL→D2 求职→D2 Python→D3 求职→D3 算法”显示，时间分别为 20:00、21:00。未把一周求职与一周学习分成两大块。
4. 第一个虚构目标点击“接受提案”后，界面显示待同步；再点击“写入每日清单”，14 项在生效计划中显示为已同步。该目标从次日开始，因此当日没有到期任务。第二个虚构目标仍停留在待本人决定的提案。

浏览器所见只能证明上述控件路径；真正的学习题目还需由用户独立完成，不能用页面存在或点击成功代替学习效果。

## 自动化检查

- `python3 -m unittest discover -s tests -q`：99 项通过。包含无勾选的原有行为、预算不足、保留求职门槛、待审提案、本人接受、每日同步、练习首次依据和本人确认正确数的虚构 HTTP 路径。
- `python3 scripts/check_privacy.py`：85 个待分享文件扫描，0 个触发项；仍需人工审阅 diff 和 Git 元数据。
- `node --check`：对页面内联脚本做语法检查。

## 仍需真实使用验证

- 真实用户愿意每天投入的时间及三项当前基础尚未确认；300 分钟是虚构验收数据，不是本人的日程。
- 现有秋招目标已经接受旧版七日计划后，不能直接在运行中补加学习项；内容变化需要新的本人确认提案入口，不能走低风险自动改期。
- 目标工作台目前不从岗位发现工作区自动取得已核验岗位，所以无岗位的提案仍是通用求职步骤；不能把它描述为已经连到真实申请。

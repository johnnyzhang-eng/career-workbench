# 同场景运行画面独立审阅

**高分屏测量链已重新核对：首轮/22:50 属于旧物理像素口径，旧指纹不作为当前验收依据。新批次已复看 B/C 白天小窗与 B800 昼夜共四张，JSON/PNG 的逻辑尺寸、原生像素与 scale=2 对应。最新有效观察见下节；其后历史记录不能自动扩展为本批次全量验收。**

2026-09-24。评审方式：通过 view_image 逐张读取运行 PNG，核对 PNG 文件头的真实尺寸；没有使用概念图替代运行图。首轮共审阅 22 张：B/C 的 1280×720 昼夜 idle、白天 started/completed；B/C 的 360×320 昼夜 idle、白天 started/completed/deferred；B/C 的 800×600 白天 idle；D 的 360×320 昼夜 idle。修正后另复看 B 的 800×600 昼夜 idle 与 B/C/D 的 360×320 白天 idle（图中时钟 22:50）。其他画面、连续动画和跨应用操作不在本次结论内。B=改良 3D，C=等距 2D，D=像素化 2D。

画面可用于比较具体缺陷，但本记录不判定最终路线，不给整组质量门槛 PASS。截图里的任务状态与按钮文字可读，角色身份、动作辨识和完整房间构图仍需局部修正。


## 最新高分屏批次复查

本次读取 B/C `360x320-day-idle`，原始 PNG 为 720×640，JSON 同时报 viewport=[360,320]、window_pixels=[720,640]、display_scale=2；B `800x600-day-idle` 与 `evening-idle` 原始 PNG 为1600×1200。view_image 展示的是原生像素图，本评审把尺寸/占屏比除以2计算逻辑口径，不把大图展示当作真实360逻辑小窗的线下可读性测试；实际屏幕显示与跨应用体验由主代理实机检查补证。

- **B800 的左墙/底座裁切在新昼夜图中均未出现。** 房间完整，任务文字及标题无截断，快捷操作无重叠；静态取景问题已在本批次复核。
- **B/C 小窗的家具标签遮挡在新图中未出现。** 电脑/日历为场景底部快捷按钮，角色与家具主体可见。按钮与实际对象的点击行为仍须对应交互测试。
- **角色占屏差异仍存在。** B 人物约50逻辑像素高、C约35–40逻辑像素高（由本图估计），两者并非等角色尺度；C橙色连帽衫、深发可辨，但背包/手势/脸部仍只占少量逻辑像素。高分屏渲染让线条更细，不增加逻辑占屏面积。此项继续作为美术限制，不能据此直接淘汰2D。
- **剩余材质与布局观察：** B白天墙/地毯仍偏亮黄，夜晚房间冷暖与明亮UI之间仍有分离；C房间细节统一，但场景在小窗画布内占比更小。下一轮先等角色尺度再比较身份和动作。

B/C本批次交互JSON均含 start、blocked_completion、complete、defer 和 proof_pass；这支持日志记录的状态序列，但本次只重新看了idle图，不把旧started/completed画面当新批次动作验收。新视频现已按下节做指定帧抽样，未做连续全帧检查。旧V04–V09只在上述重复观察到的部分延续，其余保留为待复核历史记录。

### 新批次实际审阅样本指纹

| PNG | 原始像素 | SHA-256前12位 |
| --- | --- | --- |
| 3d-360x320-day-idle.png | 720×640 | 5f7c1884a9d2 |
| 2d-360x320-day-idle.png | 720×640 | 3bf1ee87b5d0 |
| 3d-800x600-day-idle.png | 1600×1200 | 2c59d6872b2a |
| 3d-800x600-evening-idle.png | 1600×1200 | 44e0fb53c42e |

## 历史评审：先核验测量边界

已按 decision-check 检查 P1–P3：PNG 尺寸与文件名一致，360×320 用原始图检查，没有放大后宣称小窗可读；idle 与 started 的角色位置变化以及 UI 状态变化提供控制对照。完成图只代表抽中的动画相位：不得从“挥手不明显”推断动画没运行。2D 完成图能看到抬手与小星点，3D 完成图能看到姿态变化；动作持续时长、流畅性及回 idle 必须另看连续证据。

比较也存在构图变量：B 与 C 的房间、角色在同一 viewport 内所占面积不同，C 小窗角色更小；D 又在 C 构图上降低渲染分辨率。因此不能把 C/D 的细节损失直接归因于 2D 路线，更不能归因模型。本轮没有独立控制模型、修正预算或镜头倍率。

审阅时 `2d-360x320-day.json` 仅含 `collapse_restore` 事件，`3d-360x320-day.json` 含 start/blocked_completion/complete/defer；同前缀 JSON 会被另一试验覆盖。截图仍可做视觉证据，交互日志需按运行批次或试验类型分开保存，不能声称它们必然来自同一次运行。已向主代理报告。修后已核对 `2d-360x320-day.json` 恢复 start/blocked_completion/complete/defer，桌面记录另存 `2d-360x320-day-desktop.json`；本次文件覆盖问题已分开处理，桌面 flag 仍不代表跨应用行为验收。

## 阻断成品验收的问题

严重度 P1 表示本条质量门槛还不能签收，并不表示整项技术不可用。

| ID / 门槛 | 具体观察 | 证据 | 建议修正与复测 |
| --- | --- | --- | --- |
| V01 / Q04 / P1 | C/D 默认小窗中角色只有约三十余像素高，能辨认橙色人形，背包、手部和表情难以辨认；D 的脸/手进一步合成块。角色身份和温和任务反馈目前主要依赖文字 | [C 小窗](evidence/2d-360x320-day-idle.png)、[D 小窗](evidence/pixel-360x320-day-idle.png)、[C 完成](evidence/2d-360x320-day-completed.png) | 用同等角色占屏比比较 B/C/D；小窗调整镜头或独立角色近景，继续保留当前任务；按原尺寸看 idle/start/complete，不只看全窗 |
| V02 / Q03、Q07 / 已修正 | 修后 B 的 800×600 昼夜图均完整容纳左墙、底座与右侧墙端，原左缘裁切已消除，任务文字尺寸仍可读 | [B 800 白天](evidence/3d-800x600-day-idle.png)、[夜晚](evidence/3d-800x600-evening-idle.png) | 本项只确认这两个静态尺寸/时段的取景；其他尺寸不自动视为通过 |
| V03 / Q08 / 遮挡已修正，点击待验 | 修后 B/C/D 小窗把电脑/日历移为场景底部两端的快捷按钮，家具主体不再被标签覆盖；这两个按钮现在是界面快捷入口，不能再称位于物件上的热点 | [B 小窗](evidence/3d-360x320-day-idle.png)、[C 小窗](evidence/2d-360x320-day-idle.png)、[D 小窗](evidence/pixel-360x320-day-idle.png) | 小窗验证快捷按钮可用；全窗仍独立检查物件标签与点击区域，不把小窗快捷按钮当对象命中证据 |

## 美术与体验限制

| ID / 严重度 | 观察 | 证据与边界 |
| --- | --- | --- |
| V04 / P2 | B 白天墙面、地毯和地板偏亮黄，灯罩与墙面接近，材质差异主要依赖单色；夜晚有清楚的冷暖层次，但大面积浅色 UI 维持白天亮度 | [B 白天](evidence/3d-1280x720-day-idle.png)、[B 夜晚](evidence/3d-1280x720-evening-idle.png)。这不是“灯光失败”，是后续材质与夜间界面协调问题 |
| V05 / P2 | C 角色面部、抽绳和口袋在全窗清楚，房间线条统一；夜晚主要靠墙/背景变灰和窗外变深，地板、角色、桌面变化弱，缺乏明确局部暖光 | [C 白天](evidence/2d-1280x720-day-idle.png)、[C 夜晚](evidence/2d-1280x720-evening-idle.png)。未要求添加贴图资产，先调整同一套程序色彩/局部光形 |
| V06 / P2 | B started 角色已面向电脑，但仍站着背包未卸、椅子偏左；C started 角色依然面向观众举手，姿态不能直接读成键盘/学习活动。两路线都还有“状态图标式动作”的感觉 | [B started](evidence/3d-1280x720-day-started.png)、[C started](evidence/2d-1280x720-day-started.png)。截图不能判断过渡中是否穿模；不是复用先前椅子穿模结论 |
| V07 / P2 | C completed 抬手/星点全窗可见，缩到小窗后动作与 idle 很接近；B completed 也缺乏醒目的小窗轮廓差异 | [C 完成](evidence/2d-1280x720-day-completed.png)、[B 小窗完成](evidence/3d-360x320-day-completed.png)。优先改善短时动作轮廓，不增加惩罚或持续闪烁 |
| V08 / P2 | 两种全窗都是房间画布加独立任务面板，顶排还有模式/昼夜/窗口测试按钮；适合技术对照，成品 UI 与世界的融合尚未形成。小窗三操作按钮比角色显著 | B/C 全窗和小窗。测试控制可留在评审版，产品版需要另定层级；不能把对照工具 UI 当最终成品 |
| V09 / P2 | 两种房间都传达书桌/学习空间，但学生个人生活细节仍少；两者图案语言、发型、房间摆位差别明显，并非逐物体视觉同一 | [B 全窗](evidence/3d-1280x720-day-idle.png)、[C 全窗](evidence/2d-1280x720-day-idle.png)。保持同任务/身份语义可以继续研究，不能声称完全受控或已经有独特角色设定 |

## 已看到与未看到的证据

已看到：B/C 全窗和小窗任务名、状态、截止未知与主要操作可读；800×600 主标题未截断；完成显示“已记录结果”，没有冒充投递/掌握；延期显示已延期与未知截止，角色图未出现扣分、羞辱或受伤。B 当前 started 图中椅子位于角色左侧，没有从该静态画面看到原先的椅子相交；这不覆盖全段动作。D 的共用 UI 保持清晰，像素化只作用在场景上。

尚未验证：60 秒静默、完成后回 idle、全段过渡穿模、三视图、夜晚动作、小窗默认启动、收起/恢复、透明区域鼠标命中、其他应用遮挡与焦点、全屏/Mission Control/双显示器、持续资源占用。新出现的“普通窗/半透/收起”按钮只是图中可见，不证明功能有效；这些需主代理实际运行证据。

B 的 800 构图与小窗标签遮挡已在指定修后静态画面确认修正。下一步优先给所有模式相同角色占屏比并复查小窗，再看完整动作与桌面试验、比较美术和资源成本。仍保留 B/C/D 做局部复测；这些观察不足以宣布任何路线最终胜出。

## 旧样本指纹（历史记录，不作当前验收依据）

用于辨别后续覆写；均为 SHA-256 前 12 位。下面三个 idle 指纹已更新至本次复查，started/completed 保留首轮实际看过版本的指纹。

| PNG | 指纹 |
| --- | --- |
| 3d-800x600-day-idle.png | c8b7b515a614 |
| 2d-360x320-day-idle.png | 0d6c026c1cfd |
| pixel-360x320-day-idle.png | 26afd46ba867 |
| 3d-1280x720-day-started.png | 11396d88a153 |
| 2d-1280x720-day-completed.png | 69f103cf29ef |

## 新高分屏 MP4 动作抽样

独立从四段实际 MP4 解码，未使用生成前的帧序列代替视频：`3d/2d-1280x720-day-interaction.mp4` 与 `3d/2d-360x320-day-interaction.mp4`。每段取零基帧 **0、15、25、39、50、60、67**（28个样本）；另对两段全窗视频取 **0、38、44、50、56、67** 的角色局部检查肩膀/手臂（12个局部样本，部分与前组重复）。小窗抽样预览缩至360×320逻辑尺寸；全窗总览缩小用于找阶段，肩臂缺陷通过原视频局部确认。临时联系表仅用于审阅，已清理；保留原MP4及下列指纹即可按帧号复现。

| 对象 | 采样观察 | 判断边界 |
| --- | --- | --- |
| B 全窗与小窗开始 | 帧15/25相对帧0可见角色向电脑靠近并转身；椅子位于左侧，采样帧未见身体穿过椅子 | 支持“发生移动/转向”；相邻未采帧仍可能存在短暂穿模，不证明步态自然或全段无穿模 |
| B 全窗完成 / Q06 P1（旧68帧证据；已由下方V11复测修正） | 局部帧38/44/50/56右臂旋转后大部分落入橙色躯干轮廓，肩部露圆形截面；帧67恢复垂臂。完成动作没有形成清晰的外展挥手轮廓 | 这是具体肩臂旋转/遮挡缺陷，不能仅称“挥手太小”；需修枢轴/旋转方向或姿态并复测对应帧。小窗完成反馈同样难以读出 |
| C 全窗完成 | 帧38/44/50/56手臂抬到脸侧，有星点，帧67手臂回落、星点消失 | 支持短时完成反馈与最终落臂；帧60/67已切到延期任务，故不能证明保持completed状态时自动回idle的计时行为 |
| C 小窗动作 | 开始有轻微位置变化，完成有小幅抬手，帧67回垂臂；当前任务文字变化比角色动作更显著 | 角色逻辑占屏偏小的问题仍在，不因2倍物理像素而解除 |
| B/C标签与结束帧 | 采样小窗中的电脑/日历快捷按钮未遮家具；全窗标签不遮角色；延期后未见伤害/扣分或持续庆祝 | 仅覆盖采样帧与当前任务，不证明60秒安静待机或所有未来反馈分支无惩罚 |

尚不能宣布全段无穿模、完成计时自动回静止或全部质量门槛通过。特别是完成后很快切换第二任务，结束静止可能由任务切换重置动画；应补一段保持同一completed任务至少3秒的连续观察，再判定完成反馈自动结束。旧68帧B肩臂缺陷已有直接视频帧证据；新的78帧修正结果见V11。本评审不选择最终路线。

| 实际视频 | SHA-256前12位 | 编码像素/帧数/帧率 |
| --- | --- | --- |
| 3d-1280x720-day-interaction.mp4 | 03a387cdc0f6 | 2560×1440 / 68 / 10/1 |
| 3d-360x320-day-interaction.mp4 | 6a70dd7b2d31 | 720×640 / 68 / 10/1 |
| 2d-1280x720-day-interaction.mp4 | 87625234baa2 | 2560×1440 / 68 / 10/1 |
| 2d-360x320-day-interaction.mp4 | 313d991679e7 | 720×640 / 68 / 10/1 |

### V11：B 肩臂修正后复测（78帧新视频）

重新从 B 全窗2560×1440与小窗720×640 MP4各取零基帧 **38、44、56、64、69、77**，共12帧；全窗检查角色局部，小窗按360×320逻辑尺寸预览。帧38/44/56可见右臂朝身体外侧抬起，手掌端点与躯干分离，旧版肩部圆截面/手臂隐入躯干的问题在这些样本中未再出现。帧64/69右臂已垂下；小窗同期仍显示第一项“已记录结果”，直到帧77才切到第二项延期。因此这次有证据支持 **保持completed任务时，短时抬臂反馈已自动结束**，不是仅靠任务切换重置。

本项仅确认采样姿态与完成后回落；没有检查全部78帧的碰撞/穿模，也没有据此签收全部Q06。动作仍是刚性直臂外展、缺少肘腕变化，小窗清楚程度仍受角色占屏比限制。C正在重录，本节不延用旧C指纹为新视频证据；先前C审阅仅针对表中旧68帧版本。一次开始事件未触发的原因未在本评审中确认。

| 新视频 | SHA-256前12位 | 编码像素/帧数 |
| --- | --- | --- |
| 3d-1280x720-day-interaction.mp4 | 6827f3d61212 | 2560×1440 / 78 |
| 3d-360x320-day-interaction.mp4 | c3b2a5406258 | 720×640 / 78 |

### V12：C 新78帧完成后静止复核

仅追加核对 C 全窗与小窗新MP4各 **零基44、69帧**，共4帧。ffprobe确认两段均78帧，全窗2560×1440、小窗720×640。44帧可见完成抬手（全窗还有星点），69帧双手回落、星点消失；两帧UI均仍选中第一项“核验虚构岗位届别”，状态“已记录结果”。这支持C在保持completed任务时结束短时反馈；小窗手势较细，仍不据此签收角色辨识全部要求。该检查不涵盖其他76帧的连续碰撞/流畅性；本轮审阅到此结束。

| C新视频 | SHA-256前12位 | 帧数 |
| --- | --- | --- |
| 2d-1280x720-day-interaction.mp4 | fd1140e3ee9c | 78 |
| 2d-360x320-day-interaction.mp4 | 9d106b8a75e1 | 78 |
